You are auditing a style-extraction result for leakage. The extracted fields are supposed to describe VOICE ONLY — sentence rhythm, tone, structural habits — with ZERO concrete facts, numbers, project names, employer names, team names, or role titles from the original letter, even when framed as an illustrative example.

The test is NOT "is this specific to the company the letter targeted." It's "does this field contain any real, checkable content at all," regardless of whose history it describes. A true, verifiable fact about the writer's OWN past employer is still a violation if it's sitting in this file — this file has no fact-checking around it, so nothing factual belongs here no matter how stable or reusable that fact might seem. Genuinely reusable facts belong in the resume claims ledger instead, where they get verified before reuse.

Flag has_leakage=true if ANY extracted field contains a proper noun OR a concrete quoted/paraphrased fact, number, or project detail — even partially, even inside a longer phrase, even as an "e.g."

An entry that fully replaces every real name, number, and specific fact with a bracket placeholder ([entity], [metric], [process], [outcome], etc.) is NOT a violation, even if its sentence shape echoes a specific sentence from the original letter. A genericized structural template IS the intended output of extraction — templating a rhetorical move is the whole point, not a workaround to catch. Judge only what's literally still present in the text: if you can't point to an actual remaining proper noun, number, or unabstracted fact, it's not leakage, regardless of how closely the structure resembles something specific. Do not flag something "to be safe" based on structural resemblance alone.

For each real violation, return the exact field name it appeared in and the EXACT list entry as it appears in the extracted profile, copied character-for-character — not a description or paraphrase of the problem. This is used to programmatically locate and remove the entry, so it must match verbatim or it won't be found.
