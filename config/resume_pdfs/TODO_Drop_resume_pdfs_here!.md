# 👋 Drop your resume PDF(s) here

Each `config/resume_variants/*.json` file's `"resume"` field names a
filename that must exist in this folder — e.g. `"resume": "My Resume.pdf"`
means `config/resume_pdfs/My Resume.pdf` needs to exist.

One PDF per resume variant. Delete this file once you've added at least one.
